
mamba create -n sheriff_env python=3.10
conda activate sheriff_env

python3 setup.py install

mamba install typing_extensions
mamba install typer
mamba install polars
mamba install bioconda::pyranges
mamba install faiss
mamba install bioconda::gtfparse
mamba install bioconda::pysam
mamba install conda-forge::biopython

pip install gtfparse==2.5.0

## Now moving necessary data for testing

cp /Users/bradbalderson/Desktop/projects/myPython/Sheriff/example_data/blacklist_seqs.txt .

